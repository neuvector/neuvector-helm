{{- if .Values.fedmanagedservice.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: neuvector-service-controller-fed-managed
  namespace: {{ .Release.Namespace }}
  labels:
    chart: {{ template "neuvector.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ports:
  - port: 10443
    name: fed
    protocol: TCP
  type: {{ .Values.fedmanagedservice.type }}
  selector:
    app: neuvector-controller-pod
{{- end }}